# DingBot environment example

# DingTalk credentials
ACCESS_TOKEN=
SECRET=

# LLM Configuration (Gemini)
GEMINI_API_KEY=
# Optional: if using an HTTP proxy or custom endpoint for Gemini, set this. Otherwise leave empty to use the official client.
GEMINI_API_URL=
GEMINI_MODEL=models/gemini-3

# Mem0 Configuration
# Note: Mem0 uses OpenAI API by default for embeddings and memory operations
# Ensure OPENAI_API_KEY is set in environment for Mem0 to work
# export OPENAI_API_KEY=sk-xxx-yyy-zzz

# Local database
DATABASE_PATH=dingbot_memory.db
CHECK_INTERVAL_SECONDS=60
